{
  "name": "pethreon",
  "scripts": {
    "preinstall": "npx only-allow npm",
    "postinstall": "npm run build && npm run finstall",
    "dev": "npx hardhat node & npm deploy",
    "build": "npm run clean && npm run compile",
    "clean": "npx hardhat clean",
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.ts",
    "node": "npx hardhat node",
    "coverage": "npm run build && npx hardhat coverage --temp artifacts --network coverage",
    "finstall": "pushd ./frontend && pnpm install && popd",
    "fdev": "pushd ./frontend && pnpm dev && popd",
    "fbuild": "pushd ./frontend && pnpm build && popd",
    "fpreview": "pushd ./frontend && pnpm preview && popd"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.4"
  }
}
