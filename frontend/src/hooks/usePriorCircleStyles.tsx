/** 
 * 
 */
export const usePriorCircleStyles = () => {
  let circleAStyles = null
  let circleBStyles = null
  let circleCStyles = null


  return {
    circleAStyles,
    circleBStyles,
    circleCStyles,
  }
}