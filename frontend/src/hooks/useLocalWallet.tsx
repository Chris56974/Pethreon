// import { useState, useEffect } from 'react'

/** 
 * Checks indexedDB for a wallet and returns one if found.
 * You can't use localStorage to store the wallet because 
 * the wallet has a circular reference in it.
 */
export const useLocalWallet = () => {

};